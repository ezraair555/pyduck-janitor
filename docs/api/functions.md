# pyduck-janitor API reference — all functions

Complete function-by-function reference for every public method on
`DuckJanitor` plus the module-level helpers. Modeled on
[pyjanitor's API functions page](https://pyjanitor-devs.github.io/pyjanitor/api/functions/).

> Generated by `scripts/generate_api_docs.py`. Descriptions,
> parameters, returns and raises are extracted from the source
> docstrings; every example is executed before it ships. Do not edit
> by hand — regenerate instead.

## Function index

| Function | Description | Group |
| --- | --- | --- |
| [`from_pandas`](#from_pandas) | Create a DuckJanitor from a pandas DataFrame | Loaders & pipeline plumbing |
| [`from_csv`](#from_csv) | Create a DuckJanitor from a CSV file | Loaders & pipeline plumbing |
| [`from_excel`](#from_excel) | Create a DuckJanitor from an Excel file (.xlsx, .xls) | Loaders & pipeline plumbing |
| [`from_json`](#from_json) | Create a DuckJanitor from JSON / NDJSON file(s) | Loaders & pipeline plumbing |
| [`from_parquet`](#from_parquet) | Create a DuckJanitor from Parquet file(s) | Loaders & pipeline plumbing |
| [`from_sql`](#from_sql) | Create a DuckJanitor from a SQL query | Loaders & pipeline plumbing |
| [`collect`](#collect) | Execute the pipeline and return results as a pandas DataFrame | Loaders & pipeline plumbing |
| [`head`](#head) | Return the first n rows | Loaders & pipeline plumbing |
| [`sql`](#sql) | Execute a custom SQL query on the current relation | Loaders & pipeline plumbing |
| [`explain`](#explain) | Show the query plan for the current pipeline | Loaders & pipeline plumbing |
| [`get_shared_connection`](#get_shared_connection) | Get or create the global shared DuckDB connection | Loaders & pipeline plumbing |
| [`clean_names`](#clean_names) | Clean column names | Core cleaning verbs |
| [`remove_columns`](#remove_columns) | Remove specified columns | Core cleaning verbs |
| [`add_column`](#add_column) | Add a new column | Core cleaning verbs |
| [`add_columns`](#add_columns) | Alias of :meth:`add_column` accepting a dict of column→values | Core cleaning verbs |
| [`rename_column`](#rename_column) | Rename a column | Core cleaning verbs |
| [`rename_columns`](#rename_columns) | Alias of :meth:`rename_column` for the pyjanitor plural name | Core cleaning verbs |
| [`dropna`](#dropna) | Remove rows with missing values | Core cleaning verbs |
| [`remove_empty`](#remove_empty) | Remove empty rows and columns | Core cleaning verbs |
| [`filter_column`](#filter_column) | Filter rows based on column values | Core cleaning verbs |
| [`filter_column_isin`](#filter_column_isin) | Filter rows where ``column`` IS IN ``values`` | Core cleaning verbs |
| [`filter_on`](#filter_on) | Filter rows based on a SQL-like criteria string | Core cleaning verbs |
| [`filter_string`](#filter_string) | Filter rows based on whether a string column contains a substring | Core cleaning verbs |
| [`filter_date`](#filter_date) | Range-filter rows by a datetime ``column`` (R: ``filter_date``) | Core cleaning verbs |
| [`coalesce`](#coalesce) | Coalesce multiple columns into a single column | Core cleaning verbs |
| [`encode_categorical`](#encode_categorical) | Encode a column as categorical | Core cleaning verbs |
| [`get_dummies`](#get_dummies) | One-hot encode categorical columns | Core cleaning verbs |
| [`select_columns`](#select_columns) | Select specific columns | Core cleaning verbs |
| [`select`](#select) | Convenience alias for the pyjanitor ``select`` DSL | Core cleaning verbs |
| [`select_rows`](#select_rows) | Select specific rows by index or condition | Core cleaning verbs |
| [`transform_column`](#transform_column) | Transform a column using a function or SQL expression | Core cleaning verbs |
| [`transform_columns`](#transform_columns) | Transform multiple columns using a function or SQL expression | Core cleaning verbs |
| [`bin_numeric`](#bin_numeric) | Bin a numeric column into discrete intervals | Extended verbs |
| [`change_type`](#change_type) | Change the data type of a column | Extended verbs |
| [`concatenate_columns`](#concatenate_columns) | Concatenate multiple columns into a single column | Extended verbs |
| [`deconcatenate_column`](#deconcatenate_column) | Split a column into multiple columns based on a delimiter | Extended verbs |
| [`drop_constant_columns`](#drop_constant_columns) | Remove columns that have only one unique value | Extended verbs |
| [`fill`](#fill) | Fill missing values in a column | Extended verbs |
| [`fill_direction`](#fill_direction) | Alias of :meth:`fill` matching pyjanitor's name | Extended verbs |
| [`fill_empty`](#fill_empty) | Fill empty strings in a column with a specified value | Extended verbs |
| [`flag_nulls`](#flag_nulls) | Flag null values in specified columns with binary indicators | Extended verbs |
| [`limit_column_characters`](#limit_column_characters) | Limit the number of characters in a string column | Extended verbs |
| [`min_max_scale`](#min_max_scale) | Apply Min-Max scaling to a numeric column | Extended verbs |
| [`groupby_agg`](#groupby_agg) | Perform groupby aggregation | Extended verbs |
| [`groupby_topk`](#groupby_topk) | Get top k rows within each group based on a column | Extended verbs |
| [`case_when`](#case_when) | Create a column based on multiple conditions (SQL CASE WHEN) | Extended verbs |
| [`currency_column_to_numeric`](#currency_column_to_numeric) | Convert a currency column to numeric | Extended verbs |
| [`convert_date`](#convert_date) | Convert a column to date type | Extended verbs |
| [`convert_to_date`](#convert_to_date) | Alias of :meth:`convert_date` matching pyjanitor's name | Extended verbs |
| [`convert_to_datetime`](#convert_to_datetime) | Alias of :meth:`convert_date` matching pyjanitor's name | Extended verbs |
| [`convert_unix_date`](#convert_unix_date) | Coerce a UNIX/epoch numeric column into a DuckDB TIMESTAMP | Extended verbs |
| [`convert_excel_date`](#convert_excel_date) | Coerce an Excel serial date number into a DuckDB TIMESTAMP | Extended verbs |
| [`convert_matlab_date`](#convert_matlab_date) | Coerce a MATLAB serial date number into a DuckDB TIMESTAMP | Extended verbs |
| [`excel_time_to_numeric`](#excel_time_to_numeric) | Convert an Excel time-fraction column (``0.0``–``1.0``) to seconds | Extended verbs |
| [`sas_numeric_to_date`](#sas_numeric_to_date) | Convert a SAS numeric date column (days since 1960-01-01) to TIMESTAMP | Extended verbs |
| [`to_datetime`](#to_datetime) | Cast ``column`` to a TIMESTAMP using DuckDB ``strptime`` (R: ``to_datetime``) | Extended verbs |
| [`truncate_datetime`](#truncate_datetime) | Truncate a datetime column to a specified unit | Extended verbs |
| [`truncate_datetime_dataframe`](#truncate_datetime_dataframe) | Alias of :meth:`truncate_datetime` matching pyjanitor's name | Extended verbs |
| [`pivot_wider`](#pivot_wider) | Pivot data from long to wide format | Extended verbs |
| [`pivot_longer`](#pivot_longer) | Pivot data from wide to long format | Extended verbs |
| [`conditional_join`](#conditional_join) | Perform conditional (non-equi) joins | Hybrid verbs |
| [`get_dupes`](#get_dupes) | Return duplicate rows | Hybrid verbs |
| [`dropnotnull`](#dropnotnull) | Remove rows where values are NOT null (keep nulls) | Hybrid verbs |
| [`expand_column`](#expand_column) | Expand a delimited column into dummy variables | Hybrid verbs |
| [`impute`](#impute) | Impute missing values | Hybrid verbs |
| [`jitter`](#jitter) | Add random noise (jitter) to a numeric column | Hybrid verbs |
| [`label_encode`](#label_encode) | Encode categorical columns with numerical labels | Hybrid verbs |
| [`find_replace`](#find_replace) | Find and replace values in a column | Hybrid verbs |
| [`count_cumulative_unique`](#count_cumulative_unique) | Return a column with cumulative count of unique values | Hybrid verbs |
| [`complete`](#complete) | Expand relation to include all possible combinations of specified columns | Hybrid verbs |
| [`also`](#also) | Apply a Python function with side effects (materializes data) | Hybrid verbs |
| [`alias`](#alias) | Rename all columns using a string or callable | Hybrid verbs |
| [`mutate`](#mutate) | Create or modify columns using a dictionary (convenience wrapper) | Hybrid verbs |
| [`assign`](#assign) | Alias of :meth:`mutate` matching pyjanitor's name | Hybrid verbs |
| [`ungroup`](#ungroup) | Identity helper that matches pyjanitor's ``ungroup`` verb | Hybrid verbs |
| [`drop_duplicate_columns`](#drop_duplicate_columns) | Remove columns that are exact duplicates of other columns | Hybrid verbs |
| [`compare_df_cols`](#compare_df_cols) | Compare columns between two DuckJanitor instances | Hybrid verbs |
| [`compare_df_cols_same`](#compare_df_cols_same) | Compare the current relation's columns to other relations (R: ``compare_df_cols_same``) | Hybrid verbs |
| [`join_apply`](#join_apply) | Perform join then apply Python function to each row | Hybrid verbs |
| [`process_text`](#process_text) | Apply text processing function to a column | Hybrid verbs |
| [`describe_class`](#describe_class) | Describe the column types of this relation (pyjanitor ``describe_class`` parity) | Hybrid verbs |
| [`move`](#move) | Move ``source`` column relative to ``target`` column | Pyjanitor parity: structural, reshape & aggregation verbs |
| [`reorder_columns`](#reorder_columns) | Reorder the relation's columns to match ``new_order`` | Pyjanitor parity: structural, reshape & aggregation verbs |
| [`get_columns`](#get_columns) | Select columns by name (alias of :meth:`select_columns`) | Pyjanitor parity: structural, reshape & aggregation verbs |
| [`get_index_labels`](#get_index_labels) | Return the current column names as a list (label-only) | Pyjanitor parity: structural, reshape & aggregation verbs |
| [`row_to_names`](#row_to_names) | Promote one row to the column headers (R: ``row_to_names``) | Pyjanitor parity: structural, reshape & aggregation verbs |
| [`collapse_levels`](#collapse_levels) | Collapse a tuple-named index column by joining on ``sep`` (R: ``collapse_levels``) | Pyjanitor parity: structural, reshape & aggregation verbs |
| [`explode_index`](#explode_index) | Split ``column`` into multiple sub-fields (R: ``explode_index``) | Pyjanitor parity: structural, reshape & aggregation verbs |
| [`change_index_dtype`](#change_index_dtype) | Create a cast version of the FIRST column with the desired ``dtype`` (R: ``change_index_dtype``) | Pyjanitor parity: structural, reshape & aggregation verbs |
| [`expand`](#expand) | Cartesian-expand across the unique values of ``columns`` (R: ``expand``) | Pyjanitor parity: structural, reshape & aggregation verbs |
| [`expand_grid`](#expand_grid) | Cross-join an arbitrary number of relations (R: ``expand_grid``) | Pyjanitor parity: structural, reshape & aggregation verbs |
| [`summarise`](#summarise) | Group-by summarisation helper (R: ``summarise`` / ``summarize``) | Pyjanitor parity: structural, reshape & aggregation verbs |
| [`pivot_longer_spec`](#pivot_longer_spec) | Long-form pivot driven by a column-name spec (R: ``pivot_longer_spec``) | Pyjanitor parity: structural, reshape & aggregation verbs |
| [`pivot_wider_spec`](#pivot_wider_spec) | Wide pivot driven by a column-name spec (R: ``pivot_wider_spec``) | Pyjanitor parity: structural, reshape & aggregation verbs |
| [`join_agg`](#join_agg) | Aggregate join (R: ``join_agg``) — left-join with arbitrary aggregations | Pyjanitor parity: structural, reshape & aggregation verbs |
| [`get_join_indices`](#get_join_indices) | Compute join key indices without materialising the join (R: ``get_join_indices``) | Pyjanitor parity: structural, reshape & aggregation verbs |
| [`rle_id`](#rle_id) | Run-length id (R: ``rle_id``) — assign an integer id per change | Pyjanitor parity: structural, reshape & aggregation verbs |
| [`factorize_columns`](#factorize_columns) | Integer-encode each category in ``columns`` (R: ``factorize_columns``) | Pyjanitor parity: structural, reshape & aggregation verbs |
| [`update_where`](#update_where) | Update ``columns`` where a SQL ``conditions`` clause holds (R: ``update_where``) | Pyjanitor parity: structural, reshape & aggregation verbs |
| [`unionize_dataframe_categories`](#unionize_dataframe_categories) | Cast all factor-like columns across many DuckJanitors to consistent string types | Pyjanitor parity: structural, reshape & aggregation verbs |
| [`scale_mad`](#scale_mad) | Median-abs-deviation standardisation (R: ``scale_mad``) | Pyjanitor parity: structural, reshape & aggregation verbs |
| [`round_to_fraction`](#round_to_fraction) | Round ``column`` to the nearest fraction ``1/denominator`` (R: ``round_to_fraction``) | Pyjanitor parity: structural, reshape & aggregation verbs |
| [`shuffle`](#shuffle) | Return a relation with all rows in random order (R: ``shuffle``) | Pyjanitor parity: structural, reshape & aggregation verbs |
| [`toset`](#toset) | Return the unique sorted values of ``column`` as a Python list (R: ``toset``) | Pyjanitor parity: structural, reshape & aggregation verbs |
| [`take_first`](#take_first) | Return a relation containing only the first ``n`` rows (R: ``take_first``) | Pyjanitor parity: structural, reshape & aggregation verbs |
| [`sort_naturally`](#sort_naturally) | Natural-sort order for a column (R: ``sort_naturally``) | Pyjanitor parity: structural, reshape & aggregation verbs |
| [`sort_column_value_order`](#sort_column_value_order) | Sort rows by an explicit string ordering of ``column`` (R: ``sort_column_value_order``) | Pyjanitor parity: structural, reshape & aggregation verbs |
| [`cartesian_product`](#cartesian_product) | Return the cartesian (cross) product of ``self`` and ``other`` | Pyjanitor parity: structural, reshape & aggregation verbs |
| [`then`](#then) | Compose further verbs in sequence (R: ``then`` / ``DF_to_pandas``) | Pyjanitor parity: structural, reshape & aggregation verbs |
| [`DropLabel`](#DropLabel) | pyjanitor-parity label-drop sentinel for the select DSL | Module-level helpers |
| [`patterns`](#patterns) | pyjanitor-parity regex helper | Module-level helpers |

## Loaders & pipeline plumbing

<a id="from_pandas"></a>
### from_pandas

Create a DuckJanitor from a pandas DataFrame.

```python
from_pandas(df: pandas.core.frame.DataFrame) -> 'DuckJanitor'
```

**Parameters**

- **df** — pd.DataFrame
  The pandas DataFrame.

**Returns**

DuckJanitor
A DuckJanitor instance.

**Example** *(from verified snippet)*

```python
>>> df = pd.DataFrame({'a': [1, 2], 'b': ['x', 'y']})
>>> dj = DuckJanitor.from_pandas(df)
>>> dj.collect()
```



<a id="from_csv"></a>
### from_csv

Create a DuckJanitor from a CSV file.

```python
from_csv(path: Union[str, pathlib.Path], **kwargs: Any) -> 'DuckJanitor'
```

**Parameters**

- **path** — str or Path
  Path to the CSV file.
- **kwargs**
  Additional arguments passed to DuckDB's read_csv.

**Returns**

DuckJanitor
A DuckJanitor instance.

**Example** *(from verified snippet)*

```python
>>> p = make_csv({'a': [1, 2], 'b': ['x', 'y']})
>>> dj = DuckJanitor.from_csv(p)
>>> dj.collect()
```



<a id="from_excel"></a>
### from_excel

Create a DuckJanitor from an Excel file (.xlsx, .xls).

Uses pandas.read_excel as a bridge into DuckDB. This keeps the
API consistent with from_csv / from_parquet while relying on
the mature openpyxl/xlrd ecosystem for parsing.

```python
from_excel(path: Union[str, pathlib.Path], sheet_name: Union[str, int, NoneType] = 0, header: int = 0, usecols: Union[str, List[str], List[int], NoneType] = None, skiprows: Union[int, List[int], NoneType] = None, nrows: Optional[int] = None, na_values: Optional[Any] = None, keep_default_na: bool = True, dtype: Optional[Dict[str, Any]] = None, engine: Optional[str] = None, **kwargs: Any) -> 'DuckJanitor'
```

**Parameters**

- **path** — str or Path
  Path to the Excel file.
- **sheet_name** — str, int, or None, optional
  Sheet to read. 0-based index or sheet name. ``None`` reads
  all sheets (returns a dict in pandas; only the first is
  used here — pass a specific name/index to control).
- **header** — int, optional
  Row (0-indexed) to use as column names. Default 0.
- **usecols** — str, list, or None, optional
  Column selector accepted by pandas.read_excel
  (e.g. ``"A:C"``, ``["Name", "Age"]``, ``[0, 2]``).
- **skiprows** — int or list, optional
  Rows to skip at the start of the sheet.
- **nrows** — int, optional
  Number of rows to read.
- **na_values** — scalar, str, list, or dict, optional
  Additional strings to recognise as NaN.
- **keep_default_na** — bool, optional
  Whether to keep pandas' default NaN recognisers.
- **dtype** — dict, optional
  Column → dtype overrides.
- **engine** — str, optional
  'openpyxl' (xlsx) or 'xlrd' (xls). Auto-detected if omitted.
- **kwargs**
  Extra keyword args forwarded to ``pandas.read_excel``.

**Returns**

DuckJanitor
A DuckJanitor instance.

**Example** *(from docstring)*

```python
>>> dj = DuckJanitor.from_excel('data.xlsx')
>>> dj = DuckJanitor.from_excel('report.xls', sheet_name='Summary')
>>> dj = DuckJanitor.from_excel('data.xlsx', usecols='A:D', skiprows=2)
```



<a id="from_json"></a>
### from_json

Create a DuckJanitor from JSON / NDJSON file(s).

Leverages DuckDB's native ``read_json_auto`` — no pandas bridge,
so schema inference, compression detection, and glob patterns
all work out of the box.

```python
from_json(path: Union[str, pathlib.Path, List[Union[str, pathlib.Path]]], format: str = 'auto', **kwargs: Any) -> 'DuckJanitor'
```

**Parameters**

- **path** — str, Path, or list of str/Path
  Path(s) to JSON file(s). Supports:
  - Single file (``data.json``)
  - NDJSON / JSONL (``logs.jsonl``)
  - Glob patterns (``data/*.json``)
  - List of files (``['a.json', 'b.json']``)
  - Remote URLs (``s3://``, ``https://``) with httpfs loaded
  - Gzip-compressed (``.json.gz``) — auto-detected
- **format** — str, optional
  ``'auto'`` (default), ``'array'``, or ``'newline_delimited'``.
  ``'auto'`` lets DuckDB infer from file content/extension.
- **kwargs**
  Extra arguments forwarded to DuckDB's ``read_json_auto``
  (e.g. ``columns``, ``records``, ``maximum_object_size``).

**Returns**

DuckJanitor
A DuckJanitor instance.

**Example** *(from docstring)*

```python
>>> dj = DuckJanitor.from_json('data.json')
>>> dj = DuckJanitor.from_json('logs.jsonl')  # NDJSON
>>> dj = DuckJanitor.from_json('s3://bucket/data.json')
>>> dj = DuckJanitor.from_json('data/*.json')  # glob
>>> dj = DuckJanitor.from_json(['part1.json', 'part2.json'])
>>> dj = DuckJanitor.from_json('data.json', format='array')
```



<a id="from_parquet"></a>
### from_parquet

Create a DuckJanitor from Parquet file(s).

```python
from_parquet(path: Union[str, pathlib.Path, List[str]]) -> 'DuckJanitor'
```

**Parameters**

- **path** — str, Path, or list of str/Path
  Path(s) to Parquet file(s). Can be local or remote (s3://, http://).

**Returns**

DuckJanitor
A DuckJanitor instance.

**Example** *(from docstring)*

```python
>>> dj = DuckJanitor.from_parquet('data.parquet')
>>> dj = DuckJanitor.from_parquet(['part1.parquet', 'part2.parquet'])
>>> dj = DuckJanitor.from_parquet('s3://bucket/data.parquet')
```



<a id="from_sql"></a>
### from_sql

Create a DuckJanitor from a SQL query.

```python
from_sql(query: str, connection: Optional[_duckdb.DuckDBPyConnection] = None) -> 'DuckJanitor'
```

**Parameters**

- **query** — str
  SQL query to execute.
- **connection** — duckdb.DuckDBPyConnection, optional
  DuckDB connection to use.

**Returns**

DuckJanitor
A DuckJanitor instance.

**Example** *(from verified snippet)*

```python
>>> dj = DuckJanitor.from_sql("SELECT 1 AS a, 'x' AS b")
>>> dj.collect()
```



<a id="collect"></a>
### collect

Execute the pipeline and return results as a pandas DataFrame.

```python
collect(self) -> pandas.core.frame.DataFrame
```

**Returns**

pd.DataFrame
The cleaned data.

**Example** *(from docstring)*

```python
>>> result = dj.clean_names().remove_empty().collect()
```



<a id="head"></a>
### head

Return the first n rows.

```python
head(self, n: int = 5) -> pandas.core.frame.DataFrame
```

**Parameters**

- **n** — int, optional
  Number of rows. Default is 5.

**Returns**

pd.DataFrame
The first n rows.

**Example** *(from verified snippet)*

```python
>>> dj = DuckJanitor.from_pandas(df)
>>> dj.head()
```



<a id="sql"></a>
### sql

Execute a custom SQL query on the current relation.

```python
sql(self, query: str) -> 'DuckJanitor'
```

**Parameters**

- **query** — str
  SQL query. Use 'self' to refer to the current relation.

**Returns**

DuckJanitor
Self for method chaining.

**Example** *(from docstring)*

```python
>>> dj.sql("SELECT * FROM self WHERE age > 18")
```



<a id="explain"></a>
### explain

Show the query plan for the current pipeline.

```python
explain(self) -> str
```

**Returns**

str
The query plan.

**Example** *(from verified snippet)*

```python
>>> dj = DuckJanitor.from_pandas(df)
>>> dj.filter_on('a > 1').explain()
```



<a id="get_shared_connection"></a>
### get_shared_connection

Get or create the global shared DuckDB connection.

```python
get_shared_connection() -> _duckdb.DuckDBPyConnection
```

**Returns**

duckdb.DuckDBPyConnection
The shared in-memory DuckDB connection, lazily created on first call.

**Example** *(from verified snippet)*

```python
>>> dj = DuckJanitor.from_pandas(df)
>>> conn = dj.get_shared_connection()
>>> type(conn).__name__
```



## Core cleaning verbs

<a id="clean_names"></a>
### clean_names

Clean column names.

```python
clean_names(self, strip_underscores: bool = True, case_type: str = 'lower', remove_special: bool = True, snakecase: bool = True) -> 'DuckJanitor'
```

**Parameters**

- **strip_underscores** — bool, optional
  Remove leading/trailing underscores.
- **case_type** — str, optional
  Case conversion ('lower', 'upper', 'original').
- **remove_special** — bool, optional
  Remove special characters.
- **snakecase** — bool, optional
  Convert to snake_case.

**Returns**

DuckJanitor
Self for method chaining.

**Example** *(from verified snippet)*

```python
>>> dj = DuckJanitor.from_pandas(pd.DataFrame({'Sales Month': [1], 'A-B': [2]}))
>>> dj.clean_names().collect()
```



<a id="remove_columns"></a>
### remove_columns

Remove specified columns.

```python
remove_columns(self, columns: Union[str, List[str]]) -> 'DuckJanitor'
```

**Parameters**

- **columns** — str or list of str
  Column name(s) to remove.

**Returns**

DuckJanitor
Self for method chaining.

**Example** *(from verified snippet)*

```python
>>> dj = DuckJanitor.from_pandas(df)
>>> dj.remove_columns(['b']).collect()
```



<a id="add_column"></a>
### add_column

Add a new column.

```python
add_column(self, column_name: str, values: Union[Any, List[Any], str], fill_value: Optional[Any] = None) -> 'DuckJanitor'
```

**Parameters**

- **column_name** — str
  Name of the new column.
- **values** — scalar, list, or SQL expression
  Values for the column. Can be a scalar, list, or SQL expression string.
- **fill_value** — scalar, optional
  Fill value if values is shorter than DataFrame.

**Returns**

DuckJanitor
Self for method chaining.

**Example** *(from verified snippet)*

```python
>>> dj = DuckJanitor.from_pandas(df)
>>> dj.add_column('extra', [1, 2, 3, 4]).collect()
```



<a id="add_columns"></a>
### add_columns

Alias of :meth:`add_column` accepting a dict of column→values
so multiple columns can be added in a single chained call.

```python
add_columns(self, column_values: dict) -> 'DuckJanitor'
```

**Example** *(from verified snippet)*

```python
>>> dj = DuckJanitor.from_pandas(df)
>>> dj.add_columns({'extra': [1, 2, 3, 4], 'more': [9, 8, 7, 6]}).collect()
```



<a id="rename_column"></a>
### rename_column

Rename a column.

```python
rename_column(self, old_name: str, new_name: str) -> 'DuckJanitor'
```

**Parameters**

- **old_name** — str
  Current column name.
- **new_name** — str
  New column name.

**Returns**

DuckJanitor
Self for method chaining.

**Example** *(from verified snippet)*

```python
>>> dj = DuckJanitor.from_pandas(df)
>>> dj.rename_column('a', 'value').collect()
```



<a id="rename_columns"></a>
### rename_columns

Alias of :meth:`rename_column` for the pyjanitor plural name.

```python
rename_columns(self, old_name: str, new_name: str) -> 'DuckJanitor'
```

**Example** *(from verified snippet)*

```python
>>> dj = DuckJanitor.from_pandas(df)
>>> dj.rename_columns('a', 'value').collect()
```



<a id="dropna"></a>
### dropna

Remove rows with missing values.

```python
dropna(self, subset: Union[str, List[str], NoneType] = None, how: str = 'any') -> 'DuckJanitor'
```

**Parameters**

- **subset** — str or list of str, optional
  Column(s) to check for missing values.
- **how** — str, optional
  'any' or 'all' - whether to drop rows with any or all missing values.

**Returns**

DuckJanitor
Self for method chaining.

**Example** *(from verified snippet)*

```python
>>> dj2 = DuckJanitor.from_pandas(pd.DataFrame({'a': [1, None, 3], 'b': ['x', 'y', 'z']}))
>>> dj2.dropna().collect()
```



<a id="remove_empty"></a>
### remove_empty

Remove empty rows and columns.

```python
remove_empty(self) -> 'DuckJanitor'
```

**Returns**

DuckJanitor
Self for method chaining.

**Example** *(from verified snippet)*

```python
>>> dj2 = DuckJanitor.from_pandas(pd.DataFrame({'a': [1, None], 'b': [None, None]}))
>>> dj2.remove_empty().collect()
```



<a id="filter_column"></a>
### filter_column

Filter rows based on column values.

```python
filter_column(self, column: str, criteria: Union[Callable, str]) -> 'DuckJanitor'
```

**Parameters**

- **column** — str
  Column to filter on.
- **criteria** — callable or str
  Filter criteria. Can be a callable (lambda) or SQL WHERE clause string.

**Returns**

DuckJanitor
Self for method chaining.

**Example** *(from docstring)*

```python
>>> dj.filter_column('age', lambda x: x > 18)
>>> dj.filter_column('sales', 'sales > 1000')
```



<a id="filter_column_isin"></a>
### filter_column_isin

Filter rows where ``column`` IS IN ``values``.

``values`` may be any iterable of scalars. Implemented as a
direct DuckDB SQL filter because :meth:`filter_column` requires
a callable/SQL-fragment criteria.

```python
filter_column_isin(self, column: str, values, complement: bool = False) -> 'DuckJanitor'
```

**Parameters**

- **column** — str
  Name of the column to filter on.
- **values** — iterable
  Any iterable of scalars; quoted/escaped for DuckDB before
  being inlined into the SQL ``IN`` clause.
- **complement** — bool, default False
  If True, keep rows that *aren't* in ``values``.

**Returns**

DuckJanitor
Self for method chaining, restricted to matching rows.

**Example** *(from verified snippet)*

```python
>>> dj = DuckJanitor.from_pandas(df)
>>> dj.filter_column_isin('group', ['x', 'z']).collect()
```



<a id="filter_on"></a>
### filter_on

Filter rows based on a SQL-like criteria string.

```python
filter_on(self, criteria: str, complement: bool = False) -> 'DuckJanitor'
```

**Parameters**

- **criteria** — str
  A SQL WHERE-clause fragment evaluated against the current
  relation (``age > 18``); ``self`` is replaced with the
  registered relation name.
- **complement** — bool, default False
  If True, keep rows that *don't* match ``criteria``.

**Returns**

DuckJanitor
Self for method chaining, restricted to rows matching the
criteria (or non-matching if ``complement=True``).

**Example** *(from verified snippet)*

```python
>>> dj = DuckJanitor.from_pandas(df)
>>> dj.filter_on('a > 2 AND b > 15').collect()
```



<a id="filter_string"></a>
### filter_string

Filter rows based on whether a string column contains a substring.

```python
filter_string(self, column: str, search_string: str, complement: bool = False, case: bool = True, regex: bool = True) -> 'DuckJanitor'
```

**Parameters**

- **column** — str
  Name of the column to search.
- **search_string** — str
  Substring to look for. When ``regex=True`` this is a regular
  expression; otherwise it's a literal substring.
- **complement** — bool, default False
  If True, keep rows that *don't* match ``search_string``.
- **case** — bool, default True
  If False, perform case-insensitive matching.
- **regex** — bool, default True
  If False, treat ``search_string`` as a literal substring
  rather than a regular expression.

**Returns**

DuckJanitor
Self for method chaining, restricted to matching rows.

**Example** *(from verified snippet)*

```python
>>> dj = DuckJanitor.from_pandas(df)
>>> dj.filter_string('group', '^x').collect()
```



<a id="filter_date"></a>
### filter_date

Range-filter rows by a datetime ``column`` (R: ``filter_date``).

```python
filter_date(self, column: str, start_date=None, end_date=None) -> 'DuckJanitor'
```

**Parameters**

- **column** — str
  Name of the datetime column to range-filter on.
- **start_date** — str, int, float, datetime, or None, optional
  Lower bound (inclusive). ``None`` means no lower bound.
  Strings, ints and floats are passed through DuckDB's
  value-literal handling; pass ``datetime`` objects for
  type safety.
- **end_date** — str, int, float, datetime, or None, optional
  Upper bound (inclusive). ``None`` means no upper bound.

**Returns**

DuckJanitor
Self for method chaining, restricted to rows whose ``column``
falls inside ``[start_date, end_date]``.

**Example** *(from verified snippet)*

```python
>>> dj2 = DuckJanitor.from_pandas(pd.DataFrame({'d': pd.to_datetime(['2024-01-01', '2024-06-01', '2024-12-31'])}))
>>> dj2.filter_date('d', '2024-03-01', '2024-08-01').collect()
```



<a id="coalesce"></a>
### coalesce

Coalesce multiple columns into a single column.

```python
coalesce(self, columns: List[str], target_column: str) -> 'DuckJanitor'
```

**Parameters**

- **columns** — list of str
  Columns to coalesce.
- **target_column** — str
  Name of the resulting column.

**Returns**

DuckJanitor
Self for method chaining.

**Example** *(from verified snippet)*

```python
>>> dj2 = DuckJanitor.from_pandas(pd.DataFrame({'a': [1, None], 'b': [None, 2]}))
>>> dj2.coalesce(['a', 'b'], 'merged').collect()
```



<a id="encode_categorical"></a>
### encode_categorical

Encode a column as categorical.

```python
encode_categorical(self, column: str, col_name: Optional[str] = None) -> 'DuckJanitor'
```

**Parameters**

- **column** — str
  Column to encode.
- **col_name** — str, optional
  New column name. Defaults to original name.

**Returns**

DuckJanitor
Self for method chaining.

**Example** *(from verified snippet)*

```python
>>> dj = DuckJanitor.from_pandas(df)
>>> dj.encode_categorical('group').collect()
```



<a id="get_dummies"></a>
### get_dummies

One-hot encode categorical columns.

```python
get_dummies(self, columns: Union[str, List[str]], prefix: Optional[str] = None) -> 'DuckJanitor'
```

**Parameters**

- **columns** — str or list of str
  Column(s) to encode.
- **prefix** — str, optional
  Prefix for dummy column names.

**Returns**

DuckJanitor
Self for method chaining.

**Example** *(from verified snippet)*

```python
>>> dj = DuckJanitor.from_pandas(df)
>>> dj.get_dummies(['group']).collect()
```



<a id="select_columns"></a>
### select_columns

Select specific columns.

Accepts a list, a single column name, a comma-separated string,
a shell-glob pattern (e.g. ``"v*"``), or a regex prefix (``"re:^v_"``).
See :func:`pyduck_janitor.cleaning_ops.select_columns` for details.

```python
select_columns(self, columns: Union[str, List[str]]) -> 'DuckJanitor'
```

**Parameters**

- **columns** — str or list of str
  Either a single column name, a comma-separated string
  (``"a, b, c"``), a list of names/patterns, a glob
  (``"value*"``), or a regex prefix (``"re:^v_"``).
  :class:`DropLabel` may be mixed in to exclude a column.

**Returns**

DuckJanitor
Self for method chaining, restricted to the selected columns.

**Example** *(from verified snippet)*

```python
>>> dj = DuckJanitor.from_pandas(df)
>>> # comma-strings, globs and regex all work:
>>> dj.select_columns('a, group').collect()
```



<a id="select"></a>
### select

Convenience alias for the pyjanitor ``select`` DSL.

The pyjanitor API page has ``select`` at the top of the
``select`` family, but their own docs state::

    This function has been deprecated.  Kindly use
    ``jn.select_columns`` or ``jn.select_rows``.

We expose it as a chainable convenience whose only supported
form is the ``columns=`` keyword. Positional ``args`` and other
kwargs (``index=``, ``axis=``, ``invert=``) are accepted but
not implemented; they will raise ``NotImplementedError``.

```python
select(self, *args, **kwargs) -> 'DuckJanitor'
```

**Parameters**

- **args**
  Positional selector arguments. Not implemented; any
  positional arg raises ``NotImplementedError``.
- **kwargs**
  Only ``columns=`` is implemented; all other kwargs
  (``index=``, ``axis=``, ``invert=``, ``rows=``) raise
  ``NotImplementedError``.

**Returns**

DuckJanitor
Self for method chaining, restricted to the requested
columns.

**Example** *(from verified snippet)*

```python
>>> dj = DuckJanitor.from_pandas(df)
>>> dj.select(columns='a, group').collect()
```



<a id="select_rows"></a>
### select_rows

Select specific rows by index or condition.

```python
select_rows(self, indices: Union[List[int], str, NoneType] = None, criteria: Optional[str] = None) -> 'DuckJanitor'
```

**Parameters**

- **indices** — list of int or str, optional
  Either a list of 0-indexed row positions, or a SQL string
  (``"row_number() BETWEEN 1 AND 5"`` style). Mutually exclusive
  with ``criteria``.
- **criteria** — str, optional
  SQL WHERE-clause fragment (``age > 18``). Mutually exclusive
  with ``indices``.

**Returns**

DuckJanitor
Self for method chaining, restricted to the selected rows.

**Example** *(from verified snippet)*

```python
>>> dj = DuckJanitor.from_pandas(df)
>>> dj.select_rows([0, 3]).collect()
```



<a id="transform_column"></a>
### transform_column

Transform a column using a function or SQL expression.

```python
transform_column(self, column: str, func: Union[str, Callable], target_column: Optional[str] = None) -> 'DuckJanitor'
```

**Parameters**

- **column** — str
  Name of the column to transform.
- **func** — str or Callable
  Either a SQL expression referencing ``column`` (``"UPPER(column)"``)
  or a Python callable applied row-wise (column → scalar).
- **target_column** — str, optional
  Name of the output column. Defaults to overwriting ``column``
  in place.

**Returns**

DuckJanitor
Self for method chaining, with ``column`` replaced or
``target_column`` added.

**Example** *(from verified snippet)*

```python
>>> dj = DuckJanitor.from_pandas(df)
>>> dj.transform_column('a', lambda s: s * 10, 'a_x10').collect()
```



<a id="transform_columns"></a>
### transform_columns

Transform multiple columns using a function or SQL expression.

```python
transform_columns(self, columns: Union[str, List[str]], func: Union[str, Callable], target_columns: Union[str, List[str], NoneType] = None) -> 'DuckJanitor'
```

**Parameters**

- **columns** — str or list of str
  One or more column names to transform. When a single column
  is passed, ``func`` may be a Python callable; with multiple
  columns ``func`` must be a SQL expression using the same
  column names as placeholders.
- **func** — str or Callable
  SQL expression or Python callable.
- **target_columns** — str or list of str, optional
  Output column name(s). Defaults to overwriting each input
  column in place.

**Returns**

DuckJanitor
Self for method chaining, with the transformed columns.

**Example** *(from verified snippet)*

```python
>>> dj = DuckJanitor.from_pandas(df)
>>> dj.transform_columns(['a'], lambda s: s * 10, ['a_x10']).collect()
```



## Extended verbs

<a id="bin_numeric"></a>
### bin_numeric

Bin a numeric column into discrete intervals.

```python
bin_numeric(self, column: str, target_column: str, bins: Union[int, List[float]] = 5, strategy: str = 'quantile') -> 'DuckJanitor'
```

**Example** *(from verified snippet)*

```python
>>> dj = DuckJanitor.from_pandas(df)
>>> dj.bin_numeric('a', 'a_bin', bins=2).collect()
```



<a id="change_type"></a>
### change_type

Change the data type of a column.

```python
change_type(self, column: str, dtype: str) -> 'DuckJanitor'
```

**Example** *(from verified snippet)*

```python
>>> dj = DuckJanitor.from_pandas(df)
>>> dj.change_type('a', 'DOUBLE').collect()
```



<a id="concatenate_columns"></a>
### concatenate_columns

Concatenate multiple columns into a single column.

```python
concatenate_columns(self, columns: List[str], sep: str = '_', target_column: str = 'concatenated') -> 'DuckJanitor'
```

**Example** *(from verified snippet)*

```python
>>> dj = DuckJanitor.from_pandas(df)
>>> dj.concatenate_columns(['a', 'b'], '_').collect()
```



<a id="deconcatenate_column"></a>
### deconcatenate_column

Split a column into multiple columns based on a delimiter.

```python
deconcatenate_column(self, column: str, sep: str, target_columns: List[str]) -> 'DuckJanitor'
```

**Example** *(from verified snippet)*

```python
>>> dj2 = DuckJanitor.from_pandas(pd.DataFrame({'combo': ['x_1', 'y_2']}))
>>> dj2.deconcatenate_column('combo', '_', ['letter', 'number']).collect()
```



<a id="drop_constant_columns"></a>
### drop_constant_columns

Remove columns that have only one unique value.

```python
drop_constant_columns(self) -> 'DuckJanitor'
```

**Returns**

DuckJanitor
Self for method chaining.

**Example** *(from verified snippet)*

```python
>>> dj2 = DuckJanitor.from_pandas(pd.DataFrame({'a': [1, 1, 1], 'b': [1, 2, 3]}))
>>> dj2.drop_constant_columns().collect()
```



<a id="fill"></a>
### fill

Fill missing values in a column.

```python
fill(self, column: str, value: Optional[Any] = None, direction: str = 'forward', group_by: Union[str, List[str], NoneType] = None) -> 'DuckJanitor'
```

**Example** *(from verified snippet)*

```python
>>> dj2 = DuckJanitor.from_pandas(pd.DataFrame({'a': [1.0, None, None, 4.0]}))
>>> dj2.fill('a', direction='forward').collect()
```



<a id="fill_direction"></a>
### fill_direction

Alias of :meth:`fill` matching pyjanitor's name.

```python
fill_direction(self, column: str, direction: str = 'forward', value=None, **kwargs) -> 'DuckJanitor'
```

**Example** *(from verified snippet)*

```python
>>> dj2 = DuckJanitor.from_pandas(pd.DataFrame({'a': [1.0, None, None, 4.0]}))
>>> dj2.fill_direction('a', direction='forward').collect()
```



<a id="fill_empty"></a>
### fill_empty

Fill empty strings in a column with a specified value.

```python
fill_empty(self, column: str, value: str = '') -> 'DuckJanitor'
```

**Example** *(from verified snippet)*

```python
>>> dj2 = DuckJanitor.from_pandas(pd.DataFrame({'a': ['x', '', 'z']}))
>>> dj2.fill_empty('a').collect()
```



<a id="flag_nulls"></a>
### flag_nulls

Flag null values in specified columns with binary indicators.

```python
flag_nulls(self, columns: Union[str, List[str], NoneType] = None, prefix: str = 'is_null_', present_value: Any = 1, absent_value: Any = 0) -> 'DuckJanitor'
```

**Example** *(from verified snippet)*

```python
>>> dj2 = DuckJanitor.from_pandas(pd.DataFrame({'a': [1, None, 3]}))
>>> dj2.flag_nulls('a').collect()
```



<a id="limit_column_characters"></a>
### limit_column_characters

Limit the number of characters in a string column.

```python
limit_column_characters(self, column: str, max_chars: int, suffix: str = '...') -> 'DuckJanitor'
```

**Example** *(from verified snippet)*

```python
>>> dj2 = DuckJanitor.from_pandas(pd.DataFrame({'a_column_with_a_long_name': ['v']}))
>>> dj2.limit_column_characters('a_column_with_a_long_name', 6).collect()
```



<a id="min_max_scale"></a>
### min_max_scale

Apply Min-Max scaling to a numeric column.

```python
min_max_scale(self, column: str, target_column: str, min_val: float = 0, max_val: float = 1) -> 'DuckJanitor'
```

**Example** *(from verified snippet)*

```python
>>> dj = DuckJanitor.from_pandas(df)
>>> dj.min_max_scale('a', 'a_scaled').collect()
```



<a id="groupby_agg"></a>
### groupby_agg

Perform groupby aggregation.

```python
groupby_agg(self, by: Union[str, List[str]], aggregations: Dict[str, Union[str, Dict]]) -> 'DuckJanitor'
```

**Example** *(from verified snippet)*

```python
>>> dj = DuckJanitor.from_pandas(df)
>>> dj.groupby_agg('group', {'a': 'AVG', 'b': 'MAX'}).collect()
```



<a id="groupby_topk"></a>
### groupby_topk

Get top k rows within each group based on a column.

```python
groupby_topk(self, by: Union[str, List[str]], column: str, k: int, ascending: bool = False) -> 'DuckJanitor'
```

**Example** *(from verified snippet)*

```python
>>> dj = DuckJanitor.from_pandas(df)
>>> dj.groupby_topk('group', 'a', k=1).collect()
```



<a id="case_when"></a>
### case_when

Create a column based on multiple conditions (SQL CASE WHEN).

```python
case_when(self, conditions: List[tuple], target_column: str, default: Optional[Any] = None) -> 'DuckJanitor'
```

**Example** *(from verified snippet)*

```python
>>> dj = DuckJanitor.from_pandas(df)
>>> dj.case_when([('a > 2', "'high'"), ('a <= 2', "'low'")], 'level').collect()
```



<a id="currency_column_to_numeric"></a>
### currency_column_to_numeric

Convert a currency column to numeric.

```python
currency_column_to_numeric(self, column: str, target_column: Optional[str] = None) -> 'DuckJanitor'
```

**Example** *(from verified snippet)*

```python
>>> dj2 = DuckJanitor.from_pandas(pd.DataFrame({'price': ['$1,200.50', '$9.99']}))
>>> dj2.currency_column_to_numeric('price').collect()
```



<a id="convert_date"></a>
### convert_date

Convert a column to date type.

```python
convert_date(self, column: str, target_column: Optional[str] = None, date_format: Optional[str] = None) -> 'DuckJanitor'
```

**Example** *(from verified snippet)*

```python
>>> dj2 = DuckJanitor.from_pandas(pd.DataFrame({'d': ['2024-01-01', '2024-06-01']}))
>>> dj2.convert_date('d', date_format='%Y-%m-%d').collect()
```



<a id="convert_to_date"></a>
### convert_to_date

Alias of :meth:`convert_date` matching pyjanitor's name.

```python
convert_to_date(self, column: str, date_format: Optional[str] = None, target_column: Optional[str] = None, **kwargs) -> 'DuckJanitor'
```

**Example** *(from verified snippet)*

```python
>>> dj2 = DuckJanitor.from_pandas(pd.DataFrame({'d': ['2024-01-01', '2024-06-01']}))
>>> dj2.convert_to_date('d', date_format='%Y-%m-%d').collect()
```



<a id="convert_to_datetime"></a>
### convert_to_datetime

Alias of :meth:`convert_date` matching pyjanitor's name.

```python
convert_to_datetime(self, column: str, date_format: Optional[str] = None, target_column: Optional[str] = None, **kwargs) -> 'DuckJanitor'
```

**Example** *(from verified snippet)*

```python
>>> dj2 = DuckJanitor.from_pandas(pd.DataFrame({'d': ['2024-01-01 12:00', '2024-06-01 09:30']}))
>>> dj2.convert_to_datetime('d').collect()
```



<a id="convert_unix_date"></a>
### convert_unix_date

Coerce a UNIX/epoch numeric column into a DuckDB TIMESTAMP.

```python
convert_unix_date(self, column: str, unit: str = 'seconds', target_column: Optional[str] = None) -> 'DuckJanitor'
```

**Parameters**

- **column** — str
  Name of the column holding the Unix epoch value.
- **unit** — str, default 'seconds'
  One of {'seconds', 'milliseconds', 'microseconds'}.
- **target_column** — str, optional
  Name of the output column. Defaults to ``column + '_datetime'``.

**Returns**

DuckJanitor
Self for method chaining, with the new TIMESTAMP column appended.

**Example** *(from verified snippet)*

```python
>>> dj2 = DuckJanitor.from_pandas(pd.DataFrame({'epoch': [1577836800, 1704067200]}))
>>> dj2.convert_unix_date('epoch').collect()
```



<a id="convert_excel_date"></a>
### convert_excel_date

Coerce an Excel serial date number into a DuckDB TIMESTAMP.

Excel's serial date origin is 1899-12-30 (with the 1900 leap-year
bug adjustment). 1 = 1900-01-01.

```python
convert_excel_date(self, column: str, target_column: Optional[str] = None) -> 'DuckJanitor'
```

**Example** *(from verified snippet)*

```python
>>> dj2 = DuckJanitor.from_pandas(pd.DataFrame({'serial': [25569, 25569 + 30]}))
>>> dj2.convert_excel_date('serial').collect()
```



<a id="convert_matlab_date"></a>
### convert_matlab_date

Coerce a MATLAB serial date number into a DuckDB TIMESTAMP.

MATLAB datenum origin is 0000-01-01; 1 = 0000-01-01. The offset to
the DuckDB TIMESTAMP epoch (1970-01-01) is 719529 days.

```python
convert_matlab_date(self, column: str, target_column: Optional[str] = None) -> 'DuckJanitor'
```

**Example** *(from verified snippet)*

```python
>>> dj2 = DuckJanitor.from_pandas(pd.DataFrame({'serial': [719529, 719529 + 30]}))
>>> dj2.convert_matlab_date('serial').collect()
```



<a id="excel_time_to_numeric"></a>
### excel_time_to_numeric

Convert an Excel time-fraction column (``0.0``–``1.0``) to seconds.

Excel stores time-of-day as the fractional part of a day; multiplying
by ``86400`` yields seconds.

```python
excel_time_to_numeric(self, column: str, target_column: Optional[str] = None) -> 'DuckJanitor'
```

**Example** *(from verified snippet)*

```python
>>> dj2 = DuckJanitor.from_pandas(pd.DataFrame({'t': [0.25, 0.75]}))
>>> dj2.excel_time_to_numeric('t').collect()
```



<a id="sas_numeric_to_date"></a>
### sas_numeric_to_date

Convert a SAS numeric date column (days since 1960-01-01) to TIMESTAMP.

```python
sas_numeric_to_date(self, column: str, target_column: Optional[str] = None) -> 'DuckJanitor'
```

**Example** *(from verified snippet)*

```python
>>> dj2 = DuckJanitor.from_pandas(pd.DataFrame({'days': [0, 30]}))
>>> dj2.sas_numeric_to_date('d' if False else 'days').collect()
```



<a id="to_datetime"></a>
### to_datetime

Cast ``column`` to a TIMESTAMP using DuckDB ``strptime`` (R: ``to_datetime``).

```python
to_datetime(self, column: str, format: Optional[str] = None, target_column: Optional[str] = None) -> 'DuckJanitor'
```

**Example** *(from verified snippet)*

```python
>>> dj2 = DuckJanitor.from_pandas(pd.DataFrame({'d': ['2024-01-01', '2024-06-01']}))
>>> dj2.to_datetime('d', format='%Y-%m-%d').collect()
```



<a id="truncate_datetime"></a>
### truncate_datetime

Truncate a datetime column to a specified unit.

```python
truncate_datetime(self, column: str, unit: str = 'day', target_column: Optional[str] = None) -> 'DuckJanitor'
```

**Example** *(from verified snippet)*

```python
>>> dj2 = DuckJanitor.from_pandas(pd.DataFrame({'ts': pd.to_datetime(['2024-03-15 12:34', '2024-06-01 08:00'])}))
>>> dj2.truncate_datetime('ts', unit='month').collect()
```



<a id="truncate_datetime_dataframe"></a>
### truncate_datetime_dataframe

Alias of :meth:`truncate_datetime` matching pyjanitor's name.

```python
truncate_datetime_dataframe(self, column: str, unit: str = 'day', target_column: Optional[str] = None) -> 'DuckJanitor'
```

**Example** *(from verified snippet)*

```python
>>> dj2 = DuckJanitor.from_pandas(pd.DataFrame({'ts': pd.to_datetime(['2024-03-15 12:34', '2024-06-01 08:00'])}))
>>> dj2.truncate_datetime_dataframe('ts', unit='month').collect()
```



<a id="pivot_wider"></a>
### pivot_wider

Pivot data from long to wide format.

```python
pivot_wider(self, id_cols: Union[str, List[str]], name_col: str, value_col: str) -> 'DuckJanitor'
```

**Example** *(from verified snippet)*

```python
>>> dj2 = DuckJanitor.from_pandas(pd.DataFrame({'id': [1, 1, 2], 'k': ['x', 'y', 'x'], 'v': [10, 20, 30]}))
>>> dj2.pivot_wider('id', 'k', 'v').collect()
```



<a id="pivot_longer"></a>
### pivot_longer

Pivot data from wide to long format.

```python
pivot_longer(self, cols: Union[str, List[str]], names_to: str = 'variable', values_to: str = 'value') -> 'DuckJanitor'
```

**Example** *(from verified snippet)*

```python
>>> dj2 = DuckJanitor.from_pandas(pd.DataFrame({'id': [1, 2], 'x': [10, 20], 'y': [30, 40]}))
>>> dj2.pivot_longer(['x', 'y']).collect()
```



## Hybrid verbs

<a id="conditional_join"></a>
### conditional_join

Perform conditional (non-equi) joins.

```python
conditional_join(self, other: 'DuckJanitor', on: List[tuple], how: str = 'inner') -> 'DuckJanitor'
```

**Example** *(from verified snippet)*

```python
>>> left = DuckJanitor.from_pandas(pd.DataFrame({'lo': [1, 5]}))
>>> right = DuckJanitor.from_pandas(pd.DataFrame({'hi': [3, 7], 'tag': ['p', 'q']}))
>>> left.conditional_join(right, [('lo', 'hi', '<')]).collect()
```



<a id="get_dupes"></a>
### get_dupes

Return duplicate rows.

```python
get_dupes(self, columns: Union[str, List[str], NoneType] = None) -> 'DuckJanitor'
```

**Example** *(from verified snippet)*

```python
>>> dj2 = DuckJanitor.from_pandas(pd.DataFrame({'a': [1, 1, 2], 'b': ['x', 'x', 'y']}))
>>> dj2.get_dupes().collect()
```



<a id="dropnotnull"></a>
### dropnotnull

Remove rows where values are NOT null (keep nulls).

```python
dropnotnull(self, subset: Union[str, List[str], NoneType] = None, how: str = 'any') -> 'DuckJanitor'
```

**Example** *(from verified snippet)*

```python
>>> dj2 = DuckJanitor.from_pandas(pd.DataFrame({'a': [1, None], 'b': [2, 3]}))
>>> dj2.dropnotnull(subset=['a']).collect()
```



<a id="expand_column"></a>
### expand_column

Expand a delimited column into dummy variables.

```python
expand_column(self, column: str, sep: str = '|', prefix: Optional[str] = None) -> 'DuckJanitor'
```

**Example** *(from verified snippet)*

```python
>>> dj2 = DuckJanitor.from_pandas(pd.DataFrame({'tags': ['a|b', 'c|d']}))
>>> dj2.expand_column('tags').collect()
```



<a id="impute"></a>
### impute

Impute missing values.

```python
impute(self, column: str, value: Optional[Any] = None, statistic: str = 'mean', group_by: Union[str, List[str], NoneType] = None) -> 'DuckJanitor'
```

**Example** *(from verified snippet)*

```python
>>> dj2 = DuckJanitor.from_pandas(pd.DataFrame({'a': [1.0, None, 3.0]}))
>>> dj2.impute('a', statistic='mean').collect()
```



<a id="jitter"></a>
### jitter

Add random noise (jitter) to a numeric column.

```python
jitter(self, column: str, target_column: str, scale: float = 0.01, seed: Optional[int] = None) -> 'DuckJanitor'
```

**Example** *(from verified snippet)*

```python
>>> dj = DuckJanitor.from_pandas(df)
>>> dj.jitter('a', 'a_jittered', scale=0.1, seed=0).collect()
```



<a id="label_encode"></a>
### label_encode

Encode categorical columns with numerical labels.

```python
label_encode(self, columns: Union[str, List[str]], suffix: str = '_encoded') -> 'DuckJanitor'
```

**Example** *(from verified snippet)*

```python
>>> dj = DuckJanitor.from_pandas(df)
>>> dj.label_encode('group', 'group_code').collect()
```



<a id="find_replace"></a>
### find_replace

Find and replace values in a column.

```python
find_replace(self, column: str, value_pairs: Dict[str, str], target_column: Optional[str] = None) -> 'DuckJanitor'
```

**Example** *(from verified snippet)*

```python
>>> dj = DuckJanitor.from_pandas(df)
>>> dj.find_replace('group', {'x': 'X', 'z': 'Z'}).collect()
```



<a id="count_cumulative_unique"></a>
### count_cumulative_unique

Return a column with cumulative count of unique values.

```python
count_cumulative_unique(self, column: str, dest_column: str = 'cumulative_unique') -> 'DuckJanitor'
```

**Example** *(from verified snippet)*

```python
>>> dj = DuckJanitor.from_pandas(df)
>>> dj.count_cumulative_unique('group', 'ccu').collect()
```



<a id="complete"></a>
### complete

Expand relation to include all possible combinations of specified columns.

```python
complete(self, columns: Union[str, List[str]], fill_value: Any = None) -> 'DuckJanitor'
```

**Example** *(from verified snippet)*

```python
>>> dj2 = DuckJanitor.from_pandas(pd.DataFrame({'g': ['x', 'y'], 'm': [1, 2]}))
>>> dj2.complete(['g'], fill_value=0).collect()
```



<a id="also"></a>
### also

Apply a Python function with side effects (materializes data).

```python
also(self, func: Callable) -> 'DuckJanitor'
```

**Example** *(from verified snippet)*

```python
>>> dj = DuckJanitor.from_pandas(df)
>>> dj.also(lambda pdf: pdf.assign(materialized='Y')).collect()
```



<a id="alias"></a>
### alias

Rename all columns using a string or callable.

```python
alias(self, alias: Union[str, Callable]) -> 'DuckJanitor'
```

**Example** *(from verified snippet)*

```python
>>> dj = DuckJanitor.from_pandas(df)
>>> dj.alias(str.upper).collect()  # rename all columns via callable
```



<a id="mutate"></a>
### mutate

Create or modify columns using a dictionary (convenience wrapper).

```python
mutate(self, **kwargs: Any) -> 'DuckJanitor'
```

**Example** *(from verified snippet)*

```python
>>> dj = DuckJanitor.from_pandas(df)
>>> dj.mutate(double_a=[2, 4, 6, 8]).collect()
```



<a id="assign"></a>
### assign

Alias of :meth:`mutate` matching pyjanitor's name.

``assign`` accepts keyword arguments of ``column=value`` or
``column=callable``, exactly like :meth:`mutate`.

```python
assign(self, **kwargs) -> 'DuckJanitor'
```

**Example** *(from verified snippet)*

```python
>>> dj = DuckJanitor.from_pandas(df)
>>> dj.assign(double_a=[2, 4, 6, 8]).collect()
```



<a id="ungroup"></a>
### ungroup

Identity helper that matches pyjanitor's ``ungroup`` verb.

DuckDB relations are inherently ungrouped; this is a no-op that
simply returns the current DuckJanitor, kept as a chainable verb.

```python
ungroup(self, *groups, **kwargs) -> 'DuckJanitor'
```

**Example** *(from verified snippet)*

```python
>>> dj = DuckJanitor.from_pandas(df)
>>> dj.ungroup().collect()
```



<a id="drop_duplicate_columns"></a>
### drop_duplicate_columns

Remove columns that are exact duplicates of other columns.

```python
drop_duplicate_columns(self) -> 'DuckJanitor'
```

**Returns**

DuckJanitor
Self for method chaining.

**Example** *(from verified snippet)*

```python
>>> dj2 = DuckJanitor.from_pandas(pd.DataFrame({'a': [1, 2], 'b': [1, 2]}))
>>> dj2.drop_duplicate_columns().collect()
```



<a id="compare_df_cols"></a>
### compare_df_cols

Compare columns between two DuckJanitor instances.

```python
compare_df_cols(self, other: 'DuckJanitor') -> pandas.core.frame.DataFrame
```

**Example** *(from verified snippet)*

```python
>>> a = DuckJanitor.from_pandas(pd.DataFrame({'x': [1]}))
>>> b = DuckJanitor.from_pandas(pd.DataFrame({'x': [2], 'y': [3]}))
>>> compare_out = a.compare_df_cols(b)
>>> compare_out
```



<a id="compare_df_cols_same"></a>
### compare_df_cols_same

Compare the current relation's columns to other relations (R: ``compare_df_cols_same``).

```python
compare_df_cols_same(self, *others: 'DuckJanitor') -> bool
```

**Example** *(from verified snippet)*

```python
>>> a = DuckJanitor.from_pandas(pd.DataFrame({'x': [1]}))
>>> b = DuckJanitor.from_pandas(pd.DataFrame({'x': [2]}))
>>> a.compare_df_cols_same(b)
```



<a id="join_apply"></a>
### join_apply

Perform join then apply Python function to each row.

```python
join_apply(self, other: 'DuckJanitor', on: Union[str, List[str]], func: Callable, new_column_name: str) -> 'DuckJanitor'
```

**Example** *(from verified snippet)*

```python
>>> a = DuckJanitor.from_pandas(pd.DataFrame({'id': [1, 2], 'v': [10, 20]}))
>>> b = DuckJanitor.from_pandas(pd.DataFrame({'id': [1, 2], 'w': [3, 4]}))
>>> a.join_apply(b, 'id', lambda row: row['v'] + row['w'], 'v_plus_w').collect()
```



<a id="process_text"></a>
### process_text

Apply text processing function to a column.

```python
process_text(self, column: str, func: Union[Callable, str], new_column_name: str) -> 'DuckJanitor'
```

**Example** *(from verified snippet)*

```python
>>> dj = DuckJanitor.from_pandas(df)
>>> dj.process_text('group', str.upper, 'group_upper').collect()
```



<a id="describe_class"></a>
### describe_class

Describe the column types of this relation (pyjanitor ``describe_class`` parity).

```python
describe_class(self, strict_description: bool = True) -> pandas.core.frame.DataFrame
```

**Parameters**

- **strict_description** — bool, default True
  When True (default), raise a descriptive ValueError if the
  relation has no columns.  When False, return an empty frame
  instead of raising.

**Returns**

pd.DataFrame
One row per column with ``column_name`` and ``column_type``.

**Example** *(from verified snippet)*

```python
>>> dj = DuckJanitor.from_pandas(df)
>>> dj.describe_class()
```



## Pyjanitor parity: structural, reshape & aggregation verbs

<a id="move"></a>
### move

Move ``source`` column relative to ``target`` column.

position: ``'before'`` (default) or ``'after'``.

```python
move(self, source: str, target: str, position: str = 'before', **kwargs) -> 'DuckJanitor'
```

**Example** *(from verified snippet)*

```python
>>> dj = DuckJanitor.from_pandas(df)
>>> dj.move('group', 'a', position='before').collect()
```



<a id="reorder_columns"></a>
### reorder_columns

Reorder the relation's columns to match ``new_order``.

Columns not listed are dropped (matching pyjanitor's behaviour,
where columns must be enumerated fully).

```python
reorder_columns(self, new_order) -> 'DuckJanitor'
```

**Example** *(from verified snippet)*

```python
>>> dj = DuckJanitor.from_pandas(df)
>>> dj.reorder_columns(['group', 'b', 'a']).collect()
```



<a id="get_columns"></a>
### get_columns

Select columns by name (alias of :meth:`select_columns`).

```python
get_columns(self, *names) -> 'DuckJanitor'
```

**Example** *(from verified snippet)*

```python
>>> dj = DuckJanitor.from_pandas(df)
>>> dj.get_columns('a', 'group').collect()
```



<a id="get_index_labels"></a>
### get_index_labels

Return the current column names as a list (label-only).

```python
get_index_labels(self) -> List[str]
```

**Returns**

list of str
Column labels in the current DuckDB relation, in order.

**Example** *(from verified snippet)*

```python
>>> dj = DuckJanitor.from_pandas(df)
>>> dj.get_index_labels()
```



<a id="row_to_names"></a>
### row_to_names

Promote one row to the column headers (R: ``row_to_names``).

```python
row_to_names(self, row_number: int = 0, remove_row: bool = True, reset_index: bool = False) -> 'DuckJanitor'
```

**Parameters**

- **row_number** — int, default 0
  Which (0-indexed) row to lift.
- **remove_row** — bool, default True
  Drop the promoted row from the resulting body.
- **reset_index** — bool, default False
  Kept for signature parity; DuckDB relations have no integer
  index to reset.

**Returns**

DuckJanitor
Self for method chaining, with the chosen row lifted into the
column headers.

**Example** *(from verified snippet)*

```python
>>> dj2 = DuckJanitor.from_pandas(pd.DataFrame([['id', 'v'], [1, 10], [2, 20]], columns=['c1', 'c2']))
>>> dj2.row_to_names(0).collect()
```



<a id="collapse_levels"></a>
### collapse_levels

Collapse a tuple-named index column by joining on ``sep`` (R: ``collapse_levels``).

For simplicity, when ``column`` is None this concatenates all
columns together. When ``column`` is given, only that column is
collapsed (no-op since DuckDB columns are flat).

```python
collapse_levels(self, sep: str = '_', column: Optional[str] = None) -> 'DuckJanitor'
```

**Example** *(from verified snippet)*

```python
>>> dj = DuckJanitor.from_pandas(df)
>>> dj.collapse_levels(column='a').collect()
```



<a id="explode_index"></a>
### explode_index

Split ``column`` into multiple sub-fields (R: ``explode_index``).

``names`` lists the new column names. By default a single new
column called ``<column>_parsed`` is created.

```python
explode_index(self, column: str, names: Optional[List[str]] = None, separator: str = '_') -> 'DuckJanitor'
```

**Example** *(from verified snippet)*

```python
>>> dj2 = DuckJanitor.from_pandas(pd.DataFrame({'code': ['x1', 'y22']}))
>>> dj2.explode_index('code').collect()
```



<a id="change_index_dtype"></a>
### change_index_dtype

Create a cast version of the FIRST column with the desired ``dtype`` (R: ``change_index_dtype``).

DuckDB relations have no intrinsic integer index; we mimic by
projecting a typed copy of the first column.

```python
change_index_dtype(self, dtype: str, target_name: Optional[str] = None) -> 'DuckJanitor'
```

**Example** *(from verified snippet)*

```python
>>> dj = DuckJanitor.from_pandas(df)
>>> dj.change_index_dtype('VARCHAR').collect()
```



<a id="expand"></a>
### expand

Cartesian-expand across the unique values of ``columns`` (R: ``expand``).

``on`` is unused for now; in R it controls the iteration order.

```python
expand(self, columns: List[str], on=None) -> 'DuckJanitor'
```

**Example** *(from verified snippet)*

```python
>>> dj = DuckJanitor.from_pandas(df)
>>> dj.expand(['group']).collect()
```



<a id="expand_grid"></a>
### expand_grid

Cross-join an arbitrary number of relations (R: ``expand_grid``).

Each argument must be a ``DuckJanitor``. The result contains one row
for every combination of rows across all inputs (a cartesian product
in the tidyverse sense). Column names are preserved; on collision,
the later table's column is suffixed ``_1``, ``_2``, ... in order.

```python
expand_grid(self, *tables) -> 'DuckJanitor'
```

**Example** *(from verified snippet)*

```python
>>> a = DuckJanitor.from_pandas(pd.DataFrame({'x': [1, 2], 'g': ['p', 'q']}))
>>> b = DuckJanitor.from_pandas(pd.DataFrame({'y': [10, 20], 'h': ['r', 's']}))
>>> a.expand_grid(b).collect()
```



<a id="summarise"></a>
### summarise

Group-by summarisation helper (R: ``summarise`` / ``summarize``).

```python
summarise(self, group_by: Optional[List[str]] = None, agg_spec: Optional[dict] = None) -> 'DuckJanitor'
```

**Parameters**

- **group_by** — list of str
  Columns to group by; ``None`` means no grouping.
- **agg_spec** — dict
  Mapping of ``new_column_name -> (source_column, agg_function_string)``.
  Examples::
  {'avg_age': ('age', 'AVG'), 'n': ('*', 'COUNT')}

**Returns**

DuckJanitor
Self for method chaining, with the aggregated columns appended.

**Example** *(from verified snippet)*

```python
>>> dj = DuckJanitor.from_pandas(df)
>>> dj.summarise(group_by=['group'], agg_spec={'avg_a': ('a', 'AVG'), 'n': ('*', 'COUNT')}).collect()
```



<a id="pivot_longer_spec"></a>
### pivot_longer_spec

Long-form pivot driven by a column-name spec (R: ``pivot_longer_spec``).

```python
pivot_longer_spec(self, id_cols: List[str], value_cols: List[str], names_to: str = 'name', values_to: str = 'value', names_sep: Optional[str] = None) -> 'DuckJanitor'
```

**Example** *(from verified snippet)*

```python
>>> dj2 = DuckJanitor.from_pandas(pd.DataFrame({'id': [1, 2], 'y2023': [10, 20], 'y2024': [100, 200]}))
>>> dj2.pivot_longer_spec(['id'], ['y2023', 'y2024'], 'year', 'v').collect()
```



<a id="pivot_wider_spec"></a>
### pivot_wider_spec

Wide pivot driven by a column-name spec (R: ``pivot_wider_spec``).

```python
pivot_wider_spec(self, id_cols: List[str], names_from: str, values_from: str, names_glue: str = '_') -> 'DuckJanitor'
```

**Example** *(from verified snippet)*

```python
>>> dj2 = DuckJanitor.from_pandas(pd.DataFrame({'id': [1, 2], 'year': ['2023', '2024'], 'value': [10, 20]}))
>>> dj2.pivot_wider_spec(['id'], 'year', 'value').collect()
```



<a id="join_agg"></a>
### join_agg

Aggregate join (R: ``join_agg``) — left-join with arbitrary aggregations.

``aggs`` is a dict mapping ``new_col -> ("COL", AGG)``.

```python
join_agg(self, other: 'DuckJanitor', on: tuple, aggs: dict) -> 'DuckJanitor'
```

**Example** *(from verified snippet)*

```python
>>> left = DuckJanitor.from_pandas(pd.DataFrame({'lo': [1, 2]}))
>>> right = DuckJanitor.from_pandas(pd.DataFrame({'hi': [2, 3], 'w': [10, 20]}))
>>> left.join_agg(right, on=('lo', 'hi', '<'), aggs={'max_w': ('w', 'MAX')}).collect()
```



<a id="get_join_indices"></a>
### get_join_indices

Compute join key indices without materialising the join (R: ``get_join_indices``).

```python
get_join_indices(self, other: 'DuckJanitor', conditions) -> dict
```

**Example** *(from verified snippet)*

```python
>>> a = DuckJanitor.from_pandas(pd.DataFrame({'x': [1, 2]}))
>>> b = DuckJanitor.from_pandas(pd.DataFrame({'y': [2, 3]}))
>>> a.get_join_indices(b, [('x', 'y', '==')])
```



<a id="rle_id"></a>
### rle_id

Run-length id (R: ``rle_id``) — assign an integer id per change.

Implementation uses a CTE chain that hashes all columns on each row
and uses ``CONDITIONAL_TRUE_EVENT`` (via a stale flag) to break the
run-length counter when the hash changes.

```python
rle_id(self) -> 'DuckJanitor'
```

**Returns**

DuckJanitor
Self for method chaining, with an extra ``_rle_id`` column.

**Example** *(from verified snippet)*

```python
>>> dj2 = DuckJanitor.from_pandas(pd.DataFrame({'a': [1, 1, 2, 2, 1]}))
>>> dj2.rle_id().collect()
```



<a id="factorize_columns"></a>
### factorize_columns

Integer-encode each category in ``columns`` (R: ``factorize_columns``).

``columns`` defaults to all string-typed columns in the relation.

```python
factorize_columns(self, columns=None, append: bool = False) -> 'DuckJanitor'
```

**Example** *(from verified snippet)*

```python
>>> dj = DuckJanitor.from_pandas(df)
>>> dj.factorize_columns(['group']).collect()
```



<a id="update_where"></a>
### update_where

Update ``columns`` where a SQL ``conditions`` clause holds (R: ``update_where``).

``columns`` maps column-name to a SQL expression string.

```python
update_where(self, columns: dict, conditions: str) -> 'DuckJanitor'
```

**Example** *(from verified snippet)*

```python
>>> dj = DuckJanitor.from_pandas(df)
>>> dj.update_where({'group': "'Q'"}, 'a > 2').collect()
```



<a id="unionize_dataframe_categories"></a>
### unionize_dataframe_categories

Cast all factor-like columns across many DuckJanitors to consistent string types.

Useful as a preprocessing step before concat().

```python
unionize_dataframe_categories(self, *others: 'DuckJanitor', column_names=None) -> 'DuckJanitor'
```

**Example** *(from verified snippet)*

```python
>>> a = DuckJanitor.from_pandas(pd.DataFrame({'c': [1, 2]}))
>>> b = DuckJanitor.from_pandas(pd.DataFrame({'c': [3.0]}))
>>> a.unionize_dataframe_categories(b).collect()
```



<a id="scale_mad"></a>
### scale_mad

Median-abs-deviation standardisation (R: ``scale_mad``).

``by`` is one of ``'all'`` (default) or ``'column'``.

```python
scale_mad(self, column: str, by: str = 'all') -> 'DuckJanitor'
```

**Example** *(from verified snippet)*

```python
>>> dj = DuckJanitor.from_pandas(df)
>>> dj.scale_mad('a').collect()
```



<a id="round_to_fraction"></a>
### round_to_fraction

Round ``column`` to the nearest fraction ``1/denominator`` (R: ``round_to_fraction``).

```python
round_to_fraction(self, column: str, denominator: Union[int, float]) -> 'DuckJanitor'
```

**Example** *(from verified snippet)*

```python
>>> dj2 = DuckJanitor.from_pandas(pd.DataFrame({'x': [0.13, 0.55]}))
>>> dj2.round_to_fraction('x', denominator=2).collect()
```



<a id="shuffle"></a>
### shuffle

Return a relation with all rows in random order (R: ``shuffle``).

```python
shuffle(self, seed: Optional[int] = None) -> 'DuckJanitor'
```

**Parameters**

- **seed** — int, optional
  If provided, drives DuckDB's ``random()`` PRNG for reproducibility.
  The seed is normalized into ``[-1.0, 1.0]`` because DuckDB's
  ``setseed`` only accepts that range.

**Returns**

DuckJanitor
Self for method chaining.

**Example** *(from verified snippet)*

```python
>>> dj = DuckJanitor.from_pandas(df)
>>> dj.shuffle(seed=42).collect()
```



<a id="toset"></a>
### toset

Return the unique sorted values of ``column`` as a Python list (R: ``toset``).

```python
toset(self, column: str) -> list
```

**Example** *(from verified snippet)*

```python
>>> dj = DuckJanitor.from_pandas(df)
>>> dj.toset('group')
```



<a id="take_first"></a>
### take_first

Return a relation containing only the first ``n`` rows (R: ``take_first``).

```python
take_first(self, n: int = 1) -> 'DuckJanitor'
```

**Example** *(from verified snippet)*

```python
>>> dj = DuckJanitor.from_pandas(df)
>>> dj.take_first(2).collect()
```



<a id="sort_naturally"></a>
### sort_naturally

Natural-sort order for a column (R: ``sort_naturally``).

```python
sort_naturally(self, column: str) -> 'DuckJanitor'
```

**Example** *(from verified snippet)*

```python
>>> dj2 = DuckJanitor.from_pandas(pd.DataFrame({'item': ['i10', 'i2', 'i1']}))
>>> dj2.sort_naturally('item').collect()
```



<a id="sort_column_value_order"></a>
### sort_column_value_order

Sort rows by an explicit string ordering of ``column`` (R: ``sort_column_value_order``).

```python
sort_column_value_order(self, column: str, order: List[str]) -> 'DuckJanitor'
```

**Example** *(from verified snippet)*

```python
>>> dj = DuckJanitor.from_pandas(df)
>>> dj.sort_column_value_order('group', ['z', 'y', 'x']).collect()
```



<a id="cartesian_product"></a>
### cartesian_product

Return the cartesian (cross) product of ``self`` and ``other``.

```python
cartesian_product(self, other: 'DuckJanitor') -> 'DuckJanitor'
```

**Example** *(from verified snippet)*

```python
>>> a = DuckJanitor.from_pandas(pd.DataFrame({'x': [1, 2]}))
>>> b = DuckJanitor.from_pandas(pd.DataFrame({'y': ['p', 'q']}))
>>> a.cartesian_product(b).collect()
```



<a id="then"></a>
### then

Compose further verbs in sequence (R: ``then`` / ``DF_to_pandas``).

Each callable is invoked with the current DuckJanitor and must return
a DuckJanitor. Useful for ``pipe``-style chaining across modules.

```python
then(self, *funcs) -> 'DuckJanitor'
```

**Example** *(from verified snippet)*

```python
>>> dj = DuckJanitor.from_pandas(df)
>>> dj.then(lambda d: d.rename_column('a', 'value')).collect()
```



## Module-level helpers

<a id="DropLabel"></a>
### DropLabel

pyjanitor-parity label-drop sentinel for the select DSL.

Wraps a column label so that ``select_columns`` excludes it from the
output, matching pyjanitor's ``DropLabel`` dataclass semantics::

    dj.select_columns([DropLabel('unwanted'), 'keep_me'])

```python
DropLabel(label: str) -> None
```

**Example** *(from verified snippet)*

```python
>>> from pyduck_janitor import DropLabel
>>> DuckJanitor.from_pandas(df).select_columns([DropLabel('b')]).collect()
```



<a id="patterns"></a>
### patterns

pyjanitor-parity regex helper.

In pyjanitor, ``patterns(regex_pattern)`` converts a string into a
compiled regular expression for use in selection DSLs.  We subclass
``str`` so that the compiled pattern can also flow through the
``select_columns`` DSL unchanged, while still being usable directly
via ``re.search(patterns('foo'), text)``.

```python
class patterns(str)
```

**Example** *(from verified snippet)*

```python
>>> from pyduck_janitor import patterns
>>> patterns('^v').search('value') is not None
```


